登录RobloxStudio时 需开启vpn tun模式



场景报错

| 信息                                 | 含义         |
| ---------------------------------- | ---------- |
| `Script timeout`                   | 脚本运行超时     |
| `exhausted allowed execution time` | 用完了允许的执行时间 |


Lua/Luau 的设计哲学是：**一个运算符只对应一种语义**，绝不做隐式类型转换。


`do` 是 Luau 里的 **「代码块开关」**：
- 循环、条件、函数里，它是语法强制的「执行体入口」
- 手动写 `do...end`，是「隔离作用域、避免变量冲突」的神器
- 永远和 `end` 配对，顺序决定代码块边界